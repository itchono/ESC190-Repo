#include "week8.h"

void delete_node(struct LL* list,int i) {
    struct LLnode* next = NULL;

    for (int x = 0; x < i && next != NULL; x++) {
        if (x == i) {

        }
        else{

        }
    }

}
void insert_node(struct LL* list, int i, double value) {

}
void delete_list(struct LL* list) {

}

int main() {
    struct LL* queue = malloc(sizeof(struct LL));

    return 0;
}

// hashy boi?